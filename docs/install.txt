1. Install node.js with npm
https://nodejs.org/

2. Install TypeScript. Open console and run:
npm install -g typescript

3. Install TypeScript Definition(TSD) manager. Open console and run:
npm install -g tsd
