// TODO 1: generic Iterator interface

// TODO 2: generic List interface

// TODO 3: generic class ArrayIterator implementing Iterator interface

// TODO 4: generic class ArrayList implementing List interface


function genericClassesApp() {
  // TODO: uncomment

  /*
   var people : ArrayList<Person> = new ArrayList<Person>();

   people.add(new Person("Bob"));
   people.add(new Person("Ed"));
   people.add(new Person("Kate"));

   var iterator : Iterator<Person> = people.iterator();
   while (iterator.hasNext()) {
   var person : Person = iterator.next();
   console.log("next person:", person.name);
   }
   */
}

genericClassesApp();
